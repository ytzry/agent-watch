{
  "name": "agent-watch",
  "version": "1.0.0",
  "description": "Agent 会话监控 WebUI — 通过 hook 监听 Claude Code / Codex / ZCode 会话状态",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "hooks:install": "node scripts/install-hooks.js",
    "hooks:uninstall": "node scripts/uninstall-hooks.js"
  },
  "license": "ISC",
  "dependencies": {
    "express": "^5.2.1",
    "ws": "^8.21.3"
  }
}
